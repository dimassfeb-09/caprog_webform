type DocumentData = {
    id: number,
    user_id: number,
    krs: string,
    photo: string,
    ktm: string,
    ktp: string,
    dns: string,
    sertifikat: string,
    cv: string,
    folder_name: string;
}